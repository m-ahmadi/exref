<?php

/*
$mysql = new mysqli('localhost', 'PC-Fantasy', 'php1020315106016php', 'p');

run('db_p.sql');
run('table_comments.sql');
run('table_users.sql');
run('table_motherBoard/motherboard.sql');
run('table_cpu/cpu.sql');

//$mysql->errno .' : '. $mysql->error;

function run($file) {
	$handle = @fopen($file, 'r');
	if ($handle) {
		$content = @fread($handle, filesize($file));
		fclose($handle);
		$mysql->query($content);
		echo 'done';
	} else {
		echo "couldn't read the file";
	}
	
}
$mysql->close();
*/

?>