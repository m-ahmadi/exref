/*
	the value property, used by .val() always shows the current visible value,
	whereas text() return value can be wrong.
	
	.val()  always works with textarea
	.text() works sometimes and fails other times. not reliable (tried in Chrome 33)
*/