3 kinds of directive in angular

1. component directive (with a template)

2. structural directive (ngIf, ngFor)
	change the DOM layout by adding and removing DOM elements

3. attribute directives
	change the appearance or behavior of an element, component, or another directive



some examples of directives in angular


*ngIf
*ngFor
<router-outlet></router-outlet>				directives may have any selectors including the element style selector
@Input()
@Output()
@Injectable()

