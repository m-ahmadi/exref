feed forward (ffnn)		0 cycles
convolutional (cnn)		spatial-invariance, image
recurrent (rnn)			context-critical, text
generative adversarial (gan)	2 networks fight each other
encoder-decoder			built from above nets
autoencoder
reinforcement