DECIMAL or NUMERIC -- (both are same)

NUMERIC(15,2)
DECIMAL(10,2)

/*
	Since money needs an exact representation,
	don't use data types that are only approximate like float.
	use a fixed-point numeric data type like above.
*/