$('#foo').remove() // removes the element and events listeners bound to that element as well
$('#foo').detach() // removes the element without removing bound listeners
$('#foo').empty()  // remove what's inside an element