'click'             // Fired when a pointer device button (usually a mouse button) is pressed and released on the display object.
'mousedown'         // Fired when a pointer device button (usually a mouse button) is pressed on the display object.
'mousemove'         // Fired when a pointer device (usually a mouse) is moved while over the display object
'mouseout'          // Fired when a pointer device (usually a mouse) is moved off the display object
'mouseover'         // Fired when a pointer device (usually a mouse) is moved onto the display object
'mouseup'           // Fired when a pointer device button (usually a mouse button) is released over the display object.
'mouseupoutside'    // Fired when a pointer device button (usually a mouse button) is released outside the display object that initially registered a mousedown.
'pointerdown'       // Fired when a pointer device button is pressed on the display object.
'pointermove'       // Fired when a pointer device is moved while over the display object
'pointerout'        // Fired when a pointer device is moved off the display object
'pointerover'       // Fired when a pointer device is moved onto the display object
'pointertap'        // Fired when a pointer device button is pressed and released on the display object.
'pointerup'         // Fired when a pointer device button is released over the display object.
'pointerupoutside'  // Fired when a pointer device button is released outside the display object that initially registered a pointerdown.
'rightclick'        // Fired when a pointer device secondary button (usually a mouse right-button) is pressed and released on the display object.
'rightdown'         // Fired when a pointer device secondary button (usually a mouse right-button) is pressed on the display object.
'rightup'           // Fired when a pointer device secondary button (usually a mouse right-button) is released over the display object.
'rightupoutside'    // Fired when a pointer device secondary button (usually a mouse right-button) is released outside the display object that initially registered a rightdown.
'tap'               // Fired when a touch point is placed and removed from the display object.
'touchend'          // Fired when a touch point is removed from the display object.
'touchendoutside'   // Fired when a touch point is removed outside of the display object that initially registered a touchstart.
'touchmove'         // Fired when a touch point is moved along the display object.
'touchstart'        // Fired when a touch point is placed on the display object.