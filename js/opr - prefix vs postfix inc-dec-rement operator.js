var a = 1;
var b = 1;
var x;
var y;

x = a++;	// x=1, a=2 (postfix increments and returns the value before incrementing)
y = ++b;	// y=2, b=2 (prefix  increments and returns the value after  incrementing)